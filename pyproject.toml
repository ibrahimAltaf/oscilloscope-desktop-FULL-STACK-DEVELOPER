[project]
name = "oscilloscope-backend"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.109.0",
  "numpy>=1.26.0",
  "pydantic-settings>=2.0.0",
  "uvicorn[standard]>=0.27.0"
]

[project.scripts]
app = "oscilloscope_backend.api.main:app"

