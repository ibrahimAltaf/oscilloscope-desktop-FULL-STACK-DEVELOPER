# Frontend Workspace

The active Electron + React implementation is currently located at:

- `desktop/oscilloscope-desktop-main/electron`
- `desktop/oscilloscope-desktop-main/renderer`

This `frontend/` directory is the normalized root for client-facing structure and docs.
